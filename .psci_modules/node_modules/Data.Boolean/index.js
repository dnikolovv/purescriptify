"use strict";

// | An alias for `true`, which can be useful in guard clauses:
// |
// | ```purescript
// | max x y | x >= y    = x
// |         | otherwise = y
// | ```
var otherwise = true;
module.exports = {
    otherwise: otherwise
};
